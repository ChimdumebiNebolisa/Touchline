{
  "name": "touchline",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "typecheck": "npm run -ws typecheck",
    "lint": "npm run -ws lint",
    "test": "npm run -ws test",
    "manual:step1": "node scripts/manual-check-step1.mjs",
    "manual:step2": "node scripts/manual-check-step2-board-context.mjs",
    "manual:step4": "node scripts/manual-check-step4.mjs",
    "manual:step5": "node scripts/manual-check-step5-shadow-world.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "@typescript-eslint/eslint-plugin": "^8.30.1",
    "@typescript-eslint/parser": "^8.30.1",
    "eslint": "^9.24.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
